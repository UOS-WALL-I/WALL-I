spring:
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  data:
    mongodb:
      uri: mongodb+srv://minchoi:!chl153249@cluster0.gwhle.mongodb.net/myFirstDatabase?retryWrites=true&w=majority
  mail:
    host: smtp.gmail.com
    port: 587
    username: "asdf6256@gmail.com"
    password: "zikqpzlksdhhqhoj"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
logging.level:
  org.hibernate.SQL: debug
    #  org.hibernate.type: trace

server:
  servlet:
    session:
      tracking-modes: cookie
      timeout: 30m
