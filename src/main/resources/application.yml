spring:
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  data:
    mongodb:
      uri: mongodb+srv://admin:gudtlr731~!@hynam.peleb.mongodb.net/WallIDB?retryWrites=true&w=majority

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
logging.level:
  org.hibernate.SQL: debug
    #  org.hibernate.type: trace

server:
  servlet:
    session:
      tracking-modes: cookie
      timeout: 30m
